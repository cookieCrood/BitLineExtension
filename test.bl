
BOX B0 () byte {
    << [0000 0000];
}

BOX B1 () byte {
    << [1111 1111];
}

BOX NOT (
    a : bit
) bit {
    << NAND (a, a);
}

BOX AND (
    a : bit,
    b : bit
) bit {
    << NOT (AND (a, b));
}

$ AND (1, 0);